

# Change Staking Icon to Vault

**What**: Replace the `Landmark` (bank) icon with `Vault` for the Staking nav item.

**Where**: `src/constants/app.constants.ts` — change the import and the icon reference on the Staking entry from `Landmark` to `Vault`.

Single-line change, no other files affected.

