

## ✅ COMPLETED: Replace Transfer Log Discovery with API-Based Profile Discovery

Discovery now uses the DeHub API search endpoint (a-z, 0-9 prefixes) instead of scanning on-chain Transfer logs. Any user searchable in the app will be found and checked for leaderboard inclusion.

