# God's Eye View — DeHub Arcade build

Built from `DeHubToken/gods-eye-view`, itself a fork of
`bilawalsidhu/gods-eye-view`, at the commit recorded by this repository's
history. Source code is MIT licensed; see `LICENSE-GodsEyeView` at the DeHub web
repository root. Third-party data and model terms remain those documented by
the upstream project.

The build is pinned under `/gods-eye-game/`. Rebuild from the fork with
`GEV_BASE_PATH=/gods-eye-game/ npm run build` and replace this directory.
